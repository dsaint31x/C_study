 #다차원 배열
 =============

##2차원 배열 평면구조의 배열

int arrOneDim[10];       // 길이가 10인 1차원int배열


int arrOneDim[5][5];      //가로세로길이가 5인 2차원int배열


int arrOnedim[3][3][3];        //가로세로높이길이가 각3인 3차원 int배열

##2차원 배열 구조
자료형 배열이름[세로길이][가로길이]

TYPE arr[2][3]

sizeof연산자를 이용해서 2차원의 배열의 크기를 계산가능하다.
<code>
#include<stdio.h>

int main (void)
{
int arr1[3][4];
int arr2[7][9];
printf("세로3 가로4인 2차원 배열의 크기: %d\n", sizeof(arr1));
printf("세로7 가로9인 2차원 배열의 크기: %d\n", sizeof(arr2));
return 0;
}

##2차원 배열 요소의 접근 
